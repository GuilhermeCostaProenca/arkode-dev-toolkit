{
  "name": "arkode-dev-toolkit",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typegen": "openapi-typescript packages/contracts/openapi.yaml -o packages/contracts/types.gen.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.0.0",
    "turbo": "^2.0.0"
  }
}
